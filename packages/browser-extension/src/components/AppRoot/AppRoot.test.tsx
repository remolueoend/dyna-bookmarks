import { render } from "enzyme"
import { AppRoot } from "./component"

describe("components/AppRoot", () => {
  it.skip("renders correctly", () => {
    const tree = render(<AppRoot />)
    expect(tree).toBeFalsy()
  })
})
