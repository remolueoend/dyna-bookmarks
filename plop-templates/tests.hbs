import { render } from "enzyme"
import { {{name}} } from "./component"

describe("components/{{folder}}/{{name}}", () => {
  it("renders correctly", () => {
    const tree = render(<{{name}} />)
    expect(tree).toBeFalsy()
  })
})
